#include "main.h"
#include <stdio.h>

/**
 * _isupper -  checks for uppercase character.
 * @c: c -  Variable
 * Return: 0 or 1
 */
int _isupper(int c)
}
	int c = i

	if (i >= 'A' &&  i <= 'Z')
		return (1);
i	else
		return (0);
}
