#include <stdio.h>

/**
 * main - Prints with a new line
 * @argc: Argurments supplied
 * @argv: An array of pointers
 *
 * Return: Always o.
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
